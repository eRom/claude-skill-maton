"""Rule registry placeholder — rule modules will be registered here in later tasks."""

# Rules will be imported and registered here once implemented.
# Each rule module should expose a list[BaseRule] named RULES.

REGISTRY: list = []
